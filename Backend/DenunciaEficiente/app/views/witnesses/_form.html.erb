<%= form_with(model: witness, local: true) do |form| %>
  <% if witness.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(witness.errors.count, "error") %> prohibited this witness from being saved:</h2>

      <ul>
      <% witness.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :complaint_id %>
    <%= form.text_field :complaint_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
