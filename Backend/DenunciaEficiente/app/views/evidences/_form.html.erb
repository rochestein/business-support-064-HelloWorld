<%= form_with(model: evidence, local: true) do |form| %>
  <% if evidence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(evidence.errors.count, "error") %> prohibited this evidence from being saved:</h2>

      <ul>
      <% evidence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :complaint_id %>
    <%= form.text_field :complaint_id %>
  </div>

  <div class="field">
    <%= form.label :path_file %>
    <%= form.text_field :path_file %>
  </div>

  <div class="field">
    <%= form.label :narration %>
    <%= form.text_field :narration %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
