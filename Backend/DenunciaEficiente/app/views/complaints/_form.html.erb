<%= form_with(model: complaint, local: true) do |form| %>
  <% if complaint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(complaint.errors.count, "error") %> prohibited this complaint from being saved:</h2>

      <ul>
      <% complaint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :incident_id %>
    <%= form.text_field :incident_id %>
  </div>

  <div class="field">
    <%= form.label :police_unit %>
    <%= form.text_field :police_unit %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="field">
    <%= form.label :date_incident %>
    <%= form.datetime_select :date_incident %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
